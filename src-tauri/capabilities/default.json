{
  "$schema": "https://schema.tauri.app/security/2",
  "identifier": "default",
  "description": "Default capability for mini-web-sdk",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:read-all",
    "fs:write-all"
  ]
}
