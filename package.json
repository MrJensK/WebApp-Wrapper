{
  "name": "mini-web-sdk",
  "version": "0.1.0",
  "description": "Minimal Tauri v2 browser with locked download folder",
  "scripts": {
    "tauri": "tauri",
    "dev": "npx tauri dev",
    "build": "npx tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
