{
  "name": "SDK - Säker Digital Kommunikation",
  "version": "0.1.0",
  "description": "Minimal SDK App with locked download folder",
  "scripts": {
    "tauri": "npx tauri",
    "dev": "npx tauri dev",
    "build": "npx tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
